# Dependencies
node_modules/

# Build output (web + Capacitor webDir)
dist/

# Environment — never commit secrets
.env
.env.*
!.env.example

# Deploy script contains Bunny Storage password (use deploy.ps1.example)
deploy.ps1

# Generated copy of favicon for Capacitor Assets (build-android.ps1 recreates it)
assets/logo.png

# Node installer copies that sometimes land in the project folder — never commit
node.exe
nodevars.bat
npm
npm.cmd
npx
npx.cmd
corepack
corepack.cmd
install_tools.bat

# Android Studio (local machine state)
android/.idea/

# Logs & debug
*.log
npm-debug.log*

# Editor / OS
.DS_Store
Thumbs.db
*.swp
.vscode/*
!.vscode/extensions.json

# Vite
*.local

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
